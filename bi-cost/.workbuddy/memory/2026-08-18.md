# 2026-08-18

## BB 7月成本一夜 +26,404 归因分析（CDC 双写者问题）

背景：v4 查询昨晚 7 月成本 432,056.00（≈实际），今天 458,459.73（+26,404）；8 月也 +62k。5 月也曾跳 +43.4k（458,826 → 502,226）。

链路机制（flink/quantum-v2/bb）：
- DWM CDC（dwm_online_bb_card_transaction_detail_v2-cdc-sql.sql）：按**昨日事件时间窗口**（transaction_time / original_completion_time / settlement postDate）同步源库，不按 update_time 追变更。DWM 行 update_time = 源交易 update_time（COALESCE(t.update_time, t.create_time, CURRENT_TIMESTAMP)）。
- DWS CDC（dws_online_bb_card_finance_daily_v2-cdc-v2-sql.sql）：扫 DWM 近 24h update_time/delete_time 变更 → 命中 (月份+账户) 后**整月重算**，upsert（PK id=hash(月:账户:sale:am)），**不删除**（fn_delete_bb_card_finance_daily_v2_cdc 只计数不删除）。
- 月度 Batch（monthly-cdc）：fn_delete 删上月 DWS + 整月重插（writeMode='insert'），备注 bb_v2_batch_q3_refund_net_20260721。

核心风险（双写者口径漂移，最可能主因）：
- daily CDC（口径A）与 monthly Batch（口径B）指标定义不一致：Batch 有 16 个黑名单 settlement 排除（is_excluded_settlement）、auth 计数排除 business_code_list 1010、refund 按 transaction_type='refund.clearing'；CDC 无排除过滤、refund 要求 is_valid_settle、rebate base 要求 settlement_match_type='card_transaction_id'。同一 DWS 表 last-writer-wins，closed 月份被 CDC 覆盖后数值漂移。
- 结构性风险：CDC 只 upsert 不删除，若 sale/am 归属变更 → 新 id 新行 + 旧行残留 → 同月同账户双计。
- update_time 语义：DWM 行 update_time=源交易 update_time，变更扫描不可靠（漏扫/误扫）。
- 8/10 有 delete_bb_qi_dwm_dws_2026_07_08.sql（删 7/8 月 DWM+DWS）→ 重灌与 CDC 并发可能交错。

产出：online/check/v2/diag_bb_cost_jump_2026_07.sql（6 段定位脚本：remarks 来源分组 / 同账户重复行 / 近24h 7月DWM变更 / 黑名单 settlement 命中 / v4 成本分项复现）。

建议（待用户决策）：① closed 月份冻结（CDC 跳过 T-2 前月份）；② 统一 CDC 与 batch 口径；③ CDC 重算前先删 (month,account) 范围防残留重复；④ DWM update_time 改为作业写入时间；⑤ 8/10 删除+重灌操作需确认 CDC 是否已停。

## 后续定位（10:50 补充）——结论修正：不是双计，是 batch 漏写 + CDC 补写

用户跑了按 remarks 展开 + DWM 明细两条 SQL，事实：
- 15 个 CDC 账户在 DWS 7 月**没有 batch 行**（只有 active_card / fixed_fee / bb_v2_cdc 三类行）→ **batch（bb_v2_batch_q3_refund_net_20260721）重刷 7 月时漏写了这些账户**。
- DWM 明细显示这些账户 7 月窗口有大量真实交易（11622d71 达 157,584 笔、78ad30f2 10,801 笔；fa08c2ee with_sale=0 → CDC 行 sale 空，归属与 DWM 一致）→ **CDC 15 行是合法补充，不是重复**。
- 修正：之前"删 CDC 15 行"的建议作废，不能删。
- 时间线假设：8/10 手动删 7/8 月 → DWM 补回流不完整时 batch（8-17 22:39）先跑漏了 15 账户 → 8-17 晚~8-18 04:07 DWM CDC 补流 → DWS CDC 补写。
- 用户确认：delete_bb_qi_dwm_dws_2026_07_08.sql 是**用户手动处理**，不作为事故源分析。
- 待办：① 验证 DWM 7 月完整性（create_time/update_time 分布，确认迟到数据）② 决定 7 月以哪版口径为准并重新对账单 ③ 修复重灌时序（DWM 全量补回后再跑 batch）。

## 逐日化改造（11:30 完成，用户决策）

- 用户判定：DWS 表应逐日粒度（QI 已逐日正确）；BB 主链路「月初承载」是错误设计（7 月事故根源）。
- 决策：BB 主链路改逐日（对齐 QI）；active card fee 保持月初（月度去重）；fixed fee 月固定成本**按天均分**逐日分摊；历史 1-8 月**全量回刷**；monthly-cdc 用户表示暂不使用。
- 已改脚本（report_date 由 yyyy-MM-01/固定 start_date 改 CAST(x AS DATE)）：
  1. bb/cdc/dws_online_bb_card_finance_daily_v2-cdc-v2-sql.sql（changed_keys 改 event_time::date + 4 处 report_date）
  2. bb/batch/dws_online_bb_card_finance_daily_v2-batch-sql.sql（回刷工具）
  3. bb/monthly-cdc/dws_online_bb_card_finance_daily_v2-monthly-cdc-v2-sql.sql（暂不用，已改）
  4-6. fixed fee 三版（cdc/batch/monthly-cdc）：v_month_channel_cost 带 next_month + 新增 v_day_channel_cost（amount/DATEDIFF(next_month,report_month)）+ v_day_net_amount 按天 + 分摊改 day_fixed_fee×行净额/日净额
- 保持不动：active card 三版（月初）；删除函数无需改（fn_delete..._cdc 已 event_time::date；fixed fee 按月份范围删仍干净）。
- 查询侧兼容：v4 已 GROUP BY TO_CHAR(report_date,'YYYY-MM')；update_bb_cash 用 DATE_TRUNC；mv_channel_cost_daily 本身 daily 粒度用 DATE_TRUNC 关联——均兼容。
- 方案文档：plans/2026-08-18-bb-dws-daily-granularity-refactor.md（含 1-8 月回刷步骤：停 CDC → 删 DWS 1-8 月 → batch 主链路分三段跑 → fixed fee batch → active card batch → 校验 v4 → 重启 CDC）。
- 注意：batch txn 窗口仍 [月初08:00,次月月初08:00)，report_date 自然日后次月 1 日 0-8 点交易落次月 1 日（可接受差异）；fixed fee 月度总额不变仅分布到天。

## 方案 A 实现完成（14:20，T1-T5 全部落地）

- T1：transaction DWM 三版（cdc/batch/monthly-cdc）update_time 改 CAST(CURRENT_TIMESTAMP)；auth DWM 已用 ✓。解决 DWS CDC 漏触发迟到数据。
- T2：DWS CDC 主链路 v_dws_bb_txn_daily_base 对齐 batch：新增 v_bb_metric_rows_ext（16 黑名单 is_excluded_settlement）；auth 计数加 1010 排除；txn 分支 decline 置 0（由 auth 分支统一，消除双计）；reversal/refund/av/clearing vol/rebate base 全加 is_excluded_settlement=FALSE。修复 CDC 两个双计 bug（1010 进 auth+av、txn+auth decline 双计）。
- T3：fn_delete_bb_card_finance_daily_v2_cdc 由只计数改真删（p_dry_run 控制，只删普通行）；CDC 主链路加 source_delete JDBC 调用，INSERT 前 CROSS JOIN → 先删后算。
- T4：batch 主链路标注"仅初始化/回刷工具"；v_bb_txn_time_rows 窗口去掉 +8h 改自然日。
- T5（业务键五元组，用户提议）：DDL 主键改 PK(report_date, account_id, sale_id, am_id, special_fee_type)；special_fee_type 归一 'NORMAL'（NOT NULL DEFAULT）、sale_id/am_id NOT NULL DEFAULT ''；id 降级普通列。8 个 sink PK 已改五元组（主链路 cdc/batch/monthly、fixed fee 三版、active card 三版）；主链路 INSERT special_fee_type='NORMAL' + sale_id/am_id COALESCE；fixed fee 三版 v_allocation_base 改 special_fee_type='NORMAL'。删除函数保留 IS NULL OR（兼容新旧 NULL 与 'NORMAL'）。
- ⚠️ 待用户执行：**表结构变更（重建 2026 分区 + PK 五元组 + NOT NULL）**，脚本部署须在 DDL 之后；历史数据 special_fee_type NULL→'NORMAL' 在重建时处理。
- 消费侧兼容确认：v4 不按特殊类型过滤；delete 脚本 COALESCE 兼容；QI 表不动。

## CDC 复查 + 单一写入者架构（12:20，方案 A 已批准）

- 复查结论：逐日化逻辑自洽；剩余隐患 ① DWS CDC 触发依赖 DWM update_time，而 transaction DWM 的 update_time=源交易时间（迟到数据可能漏触发）② 双写者口径差异未消除（is_excluded_settlement 等 CDC 缺）③ batch txn 窗口 08:00 与自然日不一致 ④ fixed fee 须在主链路后调度。
- 用户决策：已完结月份**允许自动回补**（不冻结）；选**方案 A：CDC 单写**（DWS 唯一写入者=重构后的 daily CDC，batch 仅初始化/回刷工具）。
- 文档：plans/2026-08-18-bb-dws-cdc-single-writer-design.md（设计，已批准）、plans/2026-08-18-bb-dws-cdc-single-writer-tasks.md（实现计划 T1-T5）。
- T1 DWM update_time 改 CURRENT_TIMESTAMP（4 文件）；T2 DWS CDC 口径对齐 batch（is_excluded_settlement/1010/refund）；T3 删除函数改真删除 + CDC 先删后算；T4 batch 角色标注 + txn 窗口自然日化；T5 历史初始化（7 月应 ≈432,056）。

## DDL 执行踩坑 + IMV 检查 + DWM 重灌决策（14:30-14:46）

- **主键报错**：ADBPG 约束"主键必须包含分布键全部列"。线上 dws_bb_card_finance_daily_v2_p 分布键是 id（脚本缺 DISTRIBUTED BY，历史遗留）。解决：`ALTER TABLE ... SET DISTRIBUTED BY (report_date, account_id, sale_id, am_id, special_fee_type)` 再换主键；DDL 脚本需补 DISTRIBUTED BY 行。备选：主键含 id（防重语义失效，不推荐）/放弃业务键主键靠 T3 先删后算。
- **IMV 结论**：online/incremental-view 三个文件（mv_channel_cost_daily / mv_gross_profit_daily / mv_gross_profit_daily_aasa）SELECT 定义不用改（不引用 special_fee_type/id、逐日天然兼容、volume fee 用 DATE_TRUNC），但**必须 drop 重建**（基表主键/分布键变更 → IMV 增量状态失配；回刷整表重写 IMV 增量会炸）。顺序：drop 毛利→drop channel_cost→DDL+回刷→重建 channel_cost→重建毛利（主版或 aasa 二选一，aasa 读 dws_total_channel_cost_daily_v2_p 不走 mv_channel_cost_daily，需确认线上部署版本）→全量 REFRESH。重建后对账 special 行分组（fixed fee 每天有值，sale/am 与普通行不一致会在 IMV 拆行）。
- **DWM 重灌决策**：dwm_online_bb_card_transaction_detail_v2-batch-sql.sql 无需再改（T1 已改 update_time，取数 +8h 窗口保留是对的）。1-6 月 DWM 不用洗（8/10 删除只动 7/8 月）；**7-8 月建议洗**（重灌+CDC 补流拼的，来源不干净；重灌后 update_time=今天，重启 CDC 会扫到并重算，T2 口径已统一故无并存风险）。顺序：洗 DWM 7-8 月 → DWS DDL → DWS batch 回刷 → 校验 → 重建 IMV → 重启 CDC。

## CDC 校验错误修复（15:04）

- 报错：`Column 'settlement_match_type' not found in any table`（dws_online_bb_card_finance_daily_v2-cdc-v2-sql.sql:255-256）。
- 根因：T2 对齐 batch 口径时 bb_rebate_base_amt/bb_channel_cashback_comm 的 CASE 引用 settlement_match_type='card_transaction_id'（batch rebate 口径），但 CDC 的 source_dwm_bb_card_transaction_detail_v2_p 声明与 JDBC 投影都缺该列。
- 修复：source DDL 声明补 `settlement_match_type STRING` + JDBC 子查询投影补 `t.settlement_match_type`（用 t.txn_id, t.settlement_id 锚点精准替换，未误伤 auth source）。
- 验证：batch 主链路本就有该列（原有脚本能跑）；auth 分支不引用；sink PK 五元组/special_fee_type='NORMAL'/source_delete 均正常。

## active card / fixed fee NOT NULL 报错修复（17:33）

- 报错：`Column 'am_id' is NOT NULL, however, a null value is being written into it`（dws_online_bb_active_card_count_v2-batch-sql.sql 运行时报错）。
- 根因：T5 sink PK 改五元组后，Flink 对 PK 列（sale_id/am_id/special_fee_type）推断 NOT NULL；但 active card 与 fixed fee 的 INSERT 里 sale_id/am_id 原样透传（dim_sale_account_relation 关联可能为 NULL）→ 违反 NOT NULL。
- 修复：**6 个文件**的 sink INSERT 里 sale_id/am_id 改 `COALESCE(x, '')`（active card 三版 + fixed fee 三版，锚点 `'ACTIVE_CARD_ACCOUNT_FEE'/'CHANNEL_FIXED_FEE' AS special_fee_type,` + replace_all）。
- 验证：脚本扫描 9 个写入脚本（主链路 3 + active 3 + fixed 3）的 sink INSERT 块，无裸 sale_id/am_id；special_fee_type 全部常量非空（NORMAL/ACTIVE_CARD_ACCOUNT_FEE/CHANNEL_FIXED_FEE）。

