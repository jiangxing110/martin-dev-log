# QI 2026-08 月度系数记录 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 QI 2026-08 月度系数 seed SQL。

**Architecture:** 复用 QI 2026-07 seed 的软删除、固定 ID 更新/插入和执行后核对流程，仅替换月份、ID 和已确认参数值。

**Tech Stack:** PostgreSQL SQL、Markdown、Git。

**Spec:** `design-docs/2026-09-07-qi-month-tag-2026-08-design.md`

## Global Constraints

- 月份为 `2026-08`，统计时间为 `2026-08-01 00:00:00+08`。
- `param_e` 同时用于 4 个 CROSS 相关 tag。
- 保留 `provider='IQ'`、`product_line='QI'` 约定和 fallback 配置。

### Task 1: 新增 QI 2026-08 seed

**Files:**
- Create: `month-tag/qi_bi_month_tag_seed_2026_08.sql`

- [x] 基于 2026-07 模板生成脚本。
- [x] 写入 9 个 QI 2026-08 系数。
- [x] 保留幂等更新/插入及执行后核对。

### Task 2: 静态验证

- [x] 检查 9 个 tag、日期和参数值。
- [x] 运行 `git diff --check`。
