# 金融渠道成本按月自动重洗执行计划

## 1. 方案落地

- [ ] 把金融渠道成本脚本里的固定月份参数改成从 `ods_bi_month_tag` 自动推导。
- [ ] 统一 `v_param` / `v_month_days` 的月驱动逻辑。
- [ ] 清理脚本中剩余的硬编码月份常量。
- [ ] 删除类变更改为由编排层先清历史再重跑对应月份，脚本不再接收 `job_action`。
- [ ] 增加硬删除对账步骤，按 `ods_bi_month_tag` 与 `dwm_finance_channel_cost_p` 的月份维度做 diff。
- [ ] 落地一个独立的月份对账脚本，输出原始表与目标表的差异月份。

## 2. 验证

- [ ] 检查改造后的脚本是否还存在固定日期字面量。
- [ ] 确认单月执行路径下的 SQL 结构仍然可读。
- [ ] 确认 `source_month` 维度的主键和幂等清理逻辑没有被破坏。
- [ ] 确认删除类变更下是先清历史再重跑，没有 `job_action` 分支。
- [ ] 确认硬删除对账能稳定定位受影响月份，不误伤其它月份。

## 3. 交付

- [ ] 形成变更说明，说明脚本现在会自动覆盖 `ods_bi_month_tag` 中出现的历史月份。
