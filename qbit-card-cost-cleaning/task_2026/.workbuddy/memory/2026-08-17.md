# 2026-08-17 工作日志

## qbit_card_cost 清洗表不一致分析（task_2026）

- 任务：分析 `dws_qbit_card_transaction_2026`（清洗表）与 `qbit_card_transaction`（原始表）每日汇总的差异根因，并基于 `bi-cost/flink/quantum-v2` 写 Flink 参考实现。
- 根因（代码确认）：ETL 用 `generate_snowflake_id()` 随机主键 + `ON CONFLICT(id) DO NOTHING`（永不命中）→ 非幂等；三步法 INSERT→DELETE(create≠update)→UPDATE 重插，重跑/中断即重复追加聚合行。
- 量化（divergence_analysis.py，基于用户快照）：consumption 清洗=原始 2.27×，月度爬升 1.0→1.07→1.12→3.03→7.12→4.77（3 月初起、6-7 月高峰）；refund/reversal 基本不变（0.998/0.996）→ 膨胀集中在消费行。4-01 退款/冲正被清零；06-28、08-17 整行缺失。
- 交付物（第一版，已按用户4点重做）：
  - `analysis_report.md`（根因分析）
  - `divergence_analysis.py` + `divergence_data.json`（量化脚本与结果）
  - `flink_reference/`（按 bi-cost/online 结构 table/cdc/batch 重组）：
    - table/：transaction+extend 分表 DDL（IF NOT EXISTS 不重建）、register_fn_..._cdc_delete_v2.sql（删除函数按 createTime 年份动态路由分表 → 解决跨年）
    - cdc/：transaction + extend 各一个脚本（确定性哈希主键 id + 先删后插 writeMode=insert + 每 _YYYY 一个 SINK/INSERT 块）
    - batch/：transaction + extend 各一个修复脚本（删除函数传 p_start/p_end 整段清+重算）
- 关键设计约束（用户明确）：
  - 0) pending 长期挂起 → createTime≠updateTime → 反复进 DELETE+UPDATE 路径，非幂等主键下累积副本（消费行膨胀根因）。
  - 1) 目录结构参考 bi-cost/online（table/cdc/batch），每个 insert 表一套脚本。
  - 2) 旧结构在 task_2026/old（仅 2026 分表 DDL+作业；_2024/_2025 在线上早期脚本）。
  - 3) 表已存在、不重建不重建表、in-place 操作（复用现有 id bigint 列，只换确定性值）。
  - 4) 当前按年分表 _2024/_2025/_2026/_2027，v1 必须跨年正确处理；v2 才改成按年分区单表（去掉分表路由）。
- 待用户：库内跑校验 SQL 坐实重复行；dry-run 删除函数；决定是否按 v1 上线 + 规划 v2 分区。
- 用户要了「运行部署指南」，已写入 `flink_reference/DEPLOY.md`（阿里云实时计算 Flink 版 VVR SQL v2 部署：注册删除函数→dry-run→batch 修复→cdc 每日 BATCH 定时→停旧任务；含失败重跑数据空洞风险与 attempts=3 缓解、回滚说明、上线检查清单）。
- 用户指出「应按唯一key 删除/新增，而非整日删除」。已据此重构 v1：
  - 删除函数 `register_fn_..._cdc_delete_v2.sql` 的 CDC 分支改为按业务唯一键 `(create_date, account_id, provider, bin, business_type, status)` 精准 DELETE（PG 内用 `IS NOT DISTINCT FROM` 匹配，兼容 bin NULL，无需复现 HASH_CODE）；batch 分支仍按 create_date 区间删（修复用途）。
  - `cdc/dws_qbit_card_transaction-cdc-v2-sql.sql`：新增 `v_affected_keys` 视图，重算范围从「整天源行」收窄到「受影响业务键的当前有效源行」；SINK `writeMode` 由 insert 改为 `upsert`，确定性 `id` 主键命中即覆盖 → 真正按 key 幂等。
  - `batch/dws_qbit_card_transaction-batch-sql.sql` 写入同步改 `upsert`。
  - `restart-strategy.fixed-delay.attempts` 由 1 调到 3（防删完未插完的空窗，同一 CURRENT_DATE 窗口内自愈）。
  - 注：extend 表业务键含 currency/country 等额外维度未确认，暂保留 extend 的整日删除+insert，待用户确认其精确业务键后再镜像「按 key 删/upsert」。README/DEPLOY 的「幂等机制」段已同步更新。
