# 2026-08-18

## 脚本命名改为 bb 项目风格 (`_v2-<type>-sql.sql`)
- 用户要求 `flink_reference` 的 cdc/batch 脚本命名参考 `bi-cost/flink/quantum-v2/bb` 的 `<name>_v2-batch-sql.sql` / `<name>_v2-cdc-sql.sql` 形状。
- 生成器 `gen_all_jobs.py` 写文件处已改：
  - `cdc/<base>_v2-cdc-sql.sql`（原 `cdc/<base>-cdc-v2-sql.sql`）
  - `batch/<base>_v2-batch-sql.sql`（原 `batch-<base>-v2-sql.sql`）
- 同步更新了 README.md / DEPLOY.md 里的文件名引用。`table/` 下 DDL 与 `register_fn_*` 删除函数命名不变。
- 重生成验证：exit=0、0 报错、cdc=23/batch=23/table=46；全局回归 WHERE FALSE=0、INTERVAL 引号转义 46/46 全过。

## ⚠️ 源 SQL 位置变更（影响重生成）
- 原 `task_2026/old/insert_task_job.sql` 的 `old/` 目录已被拍平，源文件现位于 `task_2026/insert_task_job.sql`（667 行、23 个 INSERT，内容同原）。
- 生成器 SRC 已改为 `../insert_task_job.sql`；重跑前确认该文件在 `flink_reference/../` 下。

## 待用户决策（沿用昨日 upsert/删除范式讨论）
- 非 sale 表（含 `dws_qbit_card_transaction`）删除函数按「当前业务键(含 status) 精准删」，且变更窗口只抓 createTime。若 status 晚翻转（pending→closed 且 createTime 很早），旧 status 行不会被清 → 残留/重复。sale 表已改为「按(日期,账户)作用域删除」规避。要不要也把非 sale 表切到作用域删除待定。

## batch 支持传入时间 + bb 式文件头（已修）
- 问题：batch 源聚合 `affected` 窗口写死 `CURRENT_DATE - INTERVAL '1 day'`（CDC 窗口），而删除函数走修复模式按 p_start/p_end 清历史区间 → 删除一段、重算只算昨天，数据缺口。
- 修复（生成器 `gen_all_jobs.py`，已重生成 23+23）：
  - 新增 `batch_window()`：batch 用 `DATE(<create_col>) >= CAST('${start_date}' AS DATE) AND <= CAST('${end_date}' AS DATE)`，与删除函数修复模式 `create_date >= p_start AND <= p_end` 严格对齐。
  - batch 删除函数调用改为 `fn_delete_<base>_cdc(false, CAST('${start_date}' AS DATE), CAST('${end_date}' AS DATE))`。sale 表作用域 CTE 同步改 `${start_date}`/`${end_date}`。
  - 新增 `header_block()`：所有 cdc/batch 文件统一加 bb 式结构化头（Author/Created/Updated/Description/作业元信息/运行参数/Notes）。
  - 运行方式：VVR 作业参数传 `start_date`、`end_date`（YYYY-MM-DD，含两端）。
- 验证：23/23 batch 含 `${start_date}`、0 `CURRENT_DATE` 泄漏（标准表）；23/23 cdc+batch 含文件头。
- 例外：`dws_sale_transfer_extend` 仍含 `CURRENT_DATE`（cdc 里 3× 也是）。属「嵌套 FROM（LATERAL unnest 子查询）」解析器限制，from_join 把原始子查询 WHERE 原样带入 → 需人工校准 SQL，与本改动无关（本来 cdc 就坏）。该表未被 [TODO] 标记命中，检测有遗漏。

## ⚠️ 文件顺序编号范围澄清（用户纠正）
- 用户原话：「我只是改 batch 和 cdc 部分的脚本要叫 01_ 这种」→ **只给 cdc/ 与 batch/ 加 `01_` 顺序前缀（各自从 01 起、每目录 01..23），table/ 的 DDL 与 register_fn 保持原文件名、不加编号。**
- 我一度误把 table 也编成全局 01..92（cdc 47-69、batch 70-92），已纠正回生成器：cdc/batch 各自 `NN_` 前缀（per-folder 重新从 01 计数）；table 恢复原名；并清理了误加的旧全局编号文件。
- 当前磁盘：table=46（原名）、cdc=23（01_..23_）、batch=23（01_..23_）；cdc/batch 头部带 bb 式注释与 start/end 参数。
- DEPLOY.md 同步修正：section 0.5 改为「仅 cdc/batch 编号、table 不编号」；section 1.1 / 2 / 7 的文件名引用改为无编号（table）与 03_/04_（两个示例表）。
- 注意：示例表 `dws_qbit_card_transaction` 在 cdc/batch 里是 `03_`（源顺序第 3），不是 01_；别想当然写 01_。

## 清除 20/21 两张 sale 表的误报 [TODO]（已修）
- 用户指出 `20_ods_sale_fund_profits`、`21_ods_sale_qbit_card` 的 cdc/batch/删除函数都带 `-- [TODO] 检测到嵌套/非标准 FROM` 一节。
- 复核结论：这两张表是**正确**的。其 `from_join` 含 `salesAccountRelation` UNION 子查询，属 qi 式标准 sale 作用域写法；scope CTE、删除函数、重算三者共用同一 `(DATE(createTime/create_time), accountId/account_id)` 作用域与 3 通道变更窗口，别名 `tr/sar/ids` 全部能解析 → 无真实缺陷。TODO 是生成器 `nested` 启发式（含多个 ` FROM `）的**误报**。
- 生成器 `gen_all_jobs.py` 加 `VERIFIED_NESTED = {"ods_sale_fund_profits","ods_sale_qbit_card"}`：条件改为 `if (nested and base not in VERIFIED_NESTED)`，使这两张表不再出 TODO（regen 安全）。已重生成 92 文件。
- 验证：cdc/batch/table 的 [TODO] 计数均 = 0；6 个目标文件与备份 diff 仅差 TODO 行，SQL body 完全一致。
- 仍存在的真问题（与本次无关，需另排）：`dws_sale_transfer_extend` 是**真·坏**的嵌套 FROM（cdc/batch 内嵌 `CURRENT_DATE - INTERVAL`），但**没被**生成的 TODO 命中（检测漏报，false negative）。建议单独手工校准。
- 结论：生成器的 nested 检测**双向都不准**（20/21 误报、transfer_extend 漏报）；当前用 VERIFIED_NESTED 白名单兜底，未整体重写检测逻辑。

## 年份分表去掉 2027（已修）
- 用户要求：2027 表尚未生成、且 2.0 可能改分区表，故「脚本里不要出现 2027 的表」。
- 生成器 `gen_all_jobs.py` 的 `YEARS = [2024, 2025, 2026, 2027]` → `YEARS = [2024, 2025, 2026]`（含注释说明 2.0 分区表取舍）。重生成后：
  - 真实 `_2027` 表名 / sink / DDL / tableName：**全库 0 处**（彻底移除）。
  - 每文件 sink 数从 4 降到 3（_2024/_2025/_2026）；cdc/batch 各 69 个 sink。
  - 剩余 46 处 `2027` 全是 `2027-01-01` 这类**年份路由日期上界**（`create_date < DATE '2027-01-01'` 标记 2026 分区的结束），**不是 2027 表**，保留以维持正确路由（2026 窗口必须闭合，否则会吞掉 2027+ 数据）。
- 手动文件 `reset_dws_qbit_card_transaction.sql`：删掉所有 `dws_qbit_card_transaction_2027` 的 count/DELETE/TRUNCATE，并把「全部 4 个分表」改为「3 个」；顺手修了失效注释（batch 区间已从写死改为 `start_date/end_date` 参数，示例改 `CURRENT_DATE`）。
- README.md：`YEARS` 范围说明改为「[2024,2025,2026]（不含 2027）」+ 2.0 分区表取舍；另修了过时的 `../old/insert_task_job.sql` 路径（已拍平为 `../insert_task_job.sql`）。
- 取舍提示给用户：若坚持「脚本里一个 2027 字符都不出现」，可把最末年（2026）窗口改成开放区间 `create_date >= DATE '2026-01-01'`（去掉 `2027-01-01` 上界），但代价是 2027+ 数据会落入 2026 表，待 2027 表建好/regen 时再切回闭合——默认保留闭合边界（推荐）。

## ⚠️ 生成器类型错配（系统性，根因已定位）
- 用户跑 `03_dws_qbit_card_transaction` batch 报「表/列类型」错误。dws_qbit_card_transaction_2026 在 create_table_2026.sql 第47行确有（PK=id），所以不是缺表，是类型错配。
- 根因在 `gen_all_jobs.py` 的 `flink_type()`：
  - `create_date`/`date` → `"DATE"`（约352行），但物理 DDL 全是 `timestamp(6)`；
  - `transaction_count`/`count`/`version` → `"BIGINT"`（约356行），物理是 `int4`；
  - 金额/fee 类 → `"DECIMAL(20,4)"`（约367行），物理是 `numeric(18,2)`。
- 结论：**23 batch + 23 cdc 全部同款错配**（adbpg `writeMode='upsert'` 启动校验 schema 时按表名报错）。cdc/03_ 兄弟文件已 grep 确认。
- 已手工对齐 `batch/03_dws_qbit_card_transaction_v2-batch-sql.sql`：金额→DECIMAL(18,2)、count/version→INT、create_date→TIMESTAMP(6)（源侧 `TO_CHAR(...)::DATE` 改 `DATE_TRUNC('day', tr."createTime")::TIMESTAMP` 保留按天聚合）。
- 根上修复待用户确认：改 `flink_type()`（create_date→TIMESTAMP(6)、count/version→INT、金额→DECIMAL(18,2)）+ `create_date_expr()` 的 `DATE(c)`→`DATE_TRUNC('day', c)::TIMESTAMP`，再重生成全部 cdc/batch；否则 regen 会把手工改覆盖回错的。
