# 2026-08-18

## 脚本命名改为 bb 项目风格 (`_v2-<type>-sql.sql`)
- 用户要求 `flink_reference` 的 cdc/batch 脚本命名参考 `bi-cost/flink/quantum-v2/bb` 的 `<name>_v2-batch-sql.sql` / `<name>_v2-cdc-sql.sql` 形状。
- 生成器 `gen_all_jobs.py` 写文件处已改：
  - `cdc/<base>_v2-cdc-sql.sql`（原 `cdc/<base>-cdc-v2-sql.sql`）
  - `batch/<base>_v2-batch-sql.sql`（原 `batch-<base>-v2-sql.sql`）
- 同步更新了 README.md / DEPLOY.md 里的文件名引用。`table/` 下 DDL 与 `register_fn_*` 删除函数命名不变。
- 重生成验证：exit=0、0 报错、cdc=23/batch=23/table=46；全局回归 WHERE FALSE=0、INTERVAL 引号转义 46/46 全过。

## ⚠️ 源 SQL 位置变更（影响重生成）
- 原 `task_2026/old/insert_task_job.sql` 的 `old/` 目录已被拍平，源文件现位于 `task_2026/insert_task_job.sql`（667 行、23 个 INSERT，内容同原）。
- 生成器 SRC 已改为 `../insert_task_job.sql`；重跑前确认该文件在 `flink_reference/../` 下。

## 待用户决策（沿用昨日 upsert/删除范式讨论）
- 非 sale 表（含 `dws_qbit_card_transaction`）删除函数按「当前业务键(含 status) 精准删」，且变更窗口只抓 createTime。若 status 晚翻转（pending→closed 且 createTime 很早），旧 status 行不会被清 → 残留/重复。sale 表已改为「按(日期,账户)作用域删除」规避。要不要也把非 sale 表切到作用域删除待定。
