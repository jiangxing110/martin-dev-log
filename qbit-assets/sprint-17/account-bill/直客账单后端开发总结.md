# 直客账单后端开发总结

本文用于记录本次账单改造涉及的后端代码位置、职责和调用关系，便于后续排查和继续开发。

## 一、整体调用链

```text
XXL-JOB
  └─ ApiCustomerJob
      ├─ quantum_card_api_customer_bill
      ├─ quantum_card_direct_customer_bill
      ├─ create_rebate_bill
      └─ quantum_card_api_customer_netting_bill_debit
          └─ 账单生成、返现、轧差、扣款
```

## 二、定时任务入口

文件：

```text
qbit-core/src/main/java/com/qbit/job/qbitcard/ApiCustomerJob.java
```

主要任务：

| 任务 | 作用 |
|---|---|
| `quantum_card_api_customer_bill` | 生成 API 客户月结账单，同时兼容新旧费率来源 |
| `quantum_card_direct_customer_bill` | 生成直客月结账单 |
| `create_month_bill` | 原有手动/定时月结账单入口 |
| `create_rebate_bill` | 生成返现账单，直客也可以生成 |
| `quantum_card_api_customer_netting_bill_debit` | 生成或处理月账单与返现的 Net 结果 |
| `direct_customer_billing_settlement_notice` | 发送直客账单规则调整通知 |

## 三、账单生成

文件：

```text
qbit-core/src/main/java/com/qbit/common_all/api/client/bill/service/impl/ApiClientBillServiceImpl.java
```

### 3.1 API 客户

API 月结费用由以下来源合并：

```text
旧来源：api_client_transaction
新来源：account_fee_transaction
```

主要处理位置：

```text
ApiClientTransactionServiceImpl
  └─ getApiClientMonthlySettlementFee(filter)
      ├─ getApiClientMonthFees(filter)
      ├─ getMonthProvider(filter)
      ├─ api_client_transaction
      └─ account_fee_transaction
```

新旧来源最终转换成统一的 `ApiClientMonthFeeVO`，再写入：

```text
api_client_bill_statement
```

### 3.2 直客

直客月结账单只处理量子卡费用：

```text
account_fee_transaction
  └─ business_id 关联 qbitCard

accountToDo
  └─ key = ActiveCardFee
      └─ billingMonth 作为账单月份
```

主要方法：

```text
ApiClientBillServiceImpl.initDirectBill()
ApiClientBillServiceImpl.initDirectBill(accountIds, billMonths)
ApiClientBillServiceImpl.initDirectBillForMonth(...)
```

直客账单生成时写入：

```text
api_client_bill.customer_type = DIRECT
```

## 四、客户类型快照

枚举：

```text
qbit-core/src/main/java/com/qbit/common_all/quantum/card/api/enums/ApiClientBillCustomerTypeEnum.java
```

值：

```text
API
DIRECT
```

实体字段：

```text
qbit-core/src/main/java/com/qbit/openapi/domain/entity/ApiClientBill.java
```

字段：

```text
api_client_bill.customer_type
```

该字段记录账单生成时的客户类型，后续账户类型变化不影响历史账单处理。

历史账单回填 SQL：

```text
add_api_client_bill_customer_type.sql
```

位置：

```text
/Users/martinjiang/VsCodeProjects/martin-dev-log/qbit-assets/sprint-17/account-bill/
```

当前规则：

```text
ApiClient / ApiClientCustomer → API
MasterAccount / Merchant / TestAccount → DIRECT
```

## 五、费用查询和映射

Mapper：

```text
qbit-core/src/main/java/com/qbit/core/mapper/account/AccountFeeTransactionMapper.java
qbit-core/src/main/resources/mapper/AccountFeeTransactionMapper.xml
```

主要查询：

```text
selectMonthlySettlementAccountIds
selectMonthlySettlementFees
selectQuantumCardMonthlyTransactions
```

月结查询限制：

```text
deduction_node = MONTHLY
status = Closed
business_id 关联 qbitCard
fee_name 属于量子卡费用
```

费率名称映射：

```text
qbit-core/src/main/java/com/qbit/openapi/service/AccountFeeTransactionMonthlyFeeConverter.java
```

例如：

```text
QuantumCardSettlementFee → SETTLEMENT_FEE
QuantumCardAuthorizationFee → AUTH_FEE
QuantumCardActiveFee → MONTHLY_CARD_FEE
QuantumCardFxFee → FX_FEE
```

## 六、CSV 和 ZIP 导出

旧 API CSV 导出位置：

```text
qbit-core/src/main/java/com/qbit/common_all/analysis/service/impl/QbitCardTransactionStatisticServiceImpl.java
```

API 导出规则：

```text
api_client_transaction
  + account_fee_transaction
  → 合并
  → transaction_id/source_id 去重
  → 新来源优先
  → CSV
```

直客导出方法：

```text
getDirectCustomerFeeTransactionStatistics(...)
```

账单附件分流位置：

```text
ApiClientBillServiceImpl.initAttachmentCsv(...)
```

```text
customer_type = API
  → API 新旧来源 CSV

customer_type = DIRECT
  → 直客 account_fee_transaction CSV
```

ZIP 组装位置：

```text
ApiClientBillServiceImpl.initAttachmentDetailZip(...)
```

直客 ZIP 包含：

```text
直客费用 CSV
ActiveCardFee 活跃卡明细 CSV
```

活跃卡明细处理器：

```text
qbit-core/src/main/java/com/qbit/common_all/assets/service/report/csv/QuantumCardActiveCardCsvHandler.java
```

## 七、返现和 Net

返现账单生成位置：

```text
ApiClientBillServiceImpl.initRebate(...)
```

返现账单仍写入：

```text
api_client_bill.type = Rebate
```

直客返现账单生成时：

```text
api_client_bill.customer_type = DIRECT
```

Net 通过以下关系关联客户类型：

```text
api_client_netting_bill.bill_id
  → api_client_bill.customer_type
```

不在 `api_client_netting_bill` 重复增加客户类型字段。

## 八、扣款和客户端展示

Net 扣款：

```text
qbit-core/src/main/java/com/qbit/common_all/api/client/bill/service/impl/ApiClientDebitRecordServiceImpl.java
```

判断顺序：

```text
Net.bill_id
  → api_client_bill.customer_type
      ├─ DIRECT：允许量子账户扣至负数
      └─ API：使用原 API 扣款逻辑
```

客户端返现/支付展示：

```text
ApiClientBillServiceImpl.getRebateOrPay(...)
```

优先读取对应月份账单的 `customer_type`，再决定是否进入统一 Net 展示流程。

## 九、通知

通知模板 SQL：

```text
direct_customer_billing_settlement_notice.sql
```

通知枚举：

```text
qbit-core/src/main/java/com/qbit/thirdparty/internal/notice/domain/temp/NoticeTemplatesEnum.java
```

定时任务：

```text
direct_customer_billing_settlement_notice
```

当前已确认通知模板 SQL 已执行，可以发送中文和英文通知。

## 十、当前剩余验证项

- 验证 `customer_type` 回填后不存在不符合预期的 NULL。
- 验证 `TestAccount` 归类为 `DIRECT` 是否符合账单业务预期。
- 验证 API 新旧 CSV 来源重复时新来源优先。
- 验证直客 ZIP 同时包含费用 CSV 和 ActiveCardFee CSV。
- 验证直客无返现、返现大于账单、账单大于返现三种 Net 场景。
- 验证账户类型变化后历史账单仍按 `customer_type` 处理。

## 十一、推荐代码阅读顺序

如果第一次接触这部分代码，建议按下面顺序阅读，不要一开始直接从扣款方法往下看：

### 第一步：看任务入口

文件：

```text
qbit-core/src/main/java/com/qbit/job/qbitcard/ApiCustomerJob.java
```

先看这些方法：

```java
billJobHandler()
directBillJobHandler()
createBillJobHandler()
createRebateBillJobHandler()
nettingBillDebitJobHandler()
directCustomerBillingSettlementNoticeJobHandler()
```

这里的职责只有任务编排：

1. 读取 XXL-JOB 参数。
2. 记录任务开始和结束日志。
3. 调用账单 Service。
4. 不直接查询账单表和费用表。

账单的具体业务逻辑不在 `ApiCustomerJob` 中。

### 第二步：看 API 和直客账单入口

文件：

```text
qbit-core/src/main/java/com/qbit/common_all/api/client/bill/service/impl/ApiClientBillServiceImpl.java
```

#### API 入口

```java
initBill()
initApiBill(...)
createApiClientBillStatement(...)
```

执行过程：

```text
initBill()
  ├─ 计算上一个自然月的开始和结束时间
  ├─ openApiClientMapper.customerList(...)
  ├─ 排除已经生成账单的账户
  ├─ 处理优惠配置
  └─ initApiBill(...)
       ├─ 创建 api_client_bill
       ├─ 创建 api_client_bill_statement
       ├─ 写入 bill_history_config
       └─ 写入 customer_type = API
```

API 账单明细不在 `ApiClientBillServiceImpl` 内部直接拼接所有费用，而是调用：

```java
apiClientTransactionService.getApiClientMonthlySettlementFee(filter)
```

#### 直客入口

```java
initDirectBill()
initDirectBill(accountIds, billMonths)
initDirectBillForMonth(accountIds, billMonth)
```

执行过程：

```text
initDirectBill(...)
  └─ 处理账单月份
       └─ initDirectBillForMonth(...)
            ├─ 查询 account_fee_transaction 账户集合
            ├─ 查询 accountToDo.ActiveCardFee 账户集合
            ├─ 取两个来源账户 ID 的并集
            ├─ SQL 中排除 API 账户
            ├─ 查询已经生成的账单并保证幂等
            ├─ 查询 account_fee_transaction 月结费用
            ├─ 解析 ActiveCardFee
            ├─ 合并费用对象
            └─ saveDirectBills(...)
```

直客不会调用完整的 `getApiClientMonthlySettlementFee`，因为该方法会同时读取 API 旧来源 `api_client_transaction`。

### 第三步：看新费率月结费用

文件：

```text
qbit-core/src/main/java/com/qbit/openapi/service/impl/ApiClientTransactionServiceImpl.java
```

#### API 统一月结方法

```java
getApiClientMonthlySettlementFee(QueryFilterDTO filter)
```

该方法负责 API 客户新旧来源并行期间的兼容：

```text
getApiClientMonthlySettlementFee
  ├─ getApiClientMonthFees(filter)
  │    ├─ 旧 api_client_transaction 汇总
  │    └─ 新 account_fee_transaction 汇总
  ├─ getMonthProvider(filter)
  │    ├─ 旧 provider 汇总
  │    └─ 新 account_fee_transaction provider 汇总
  ├─ getMonthProviderExtends(filter)
  ├─ qbitAccountIncomeMap(filter)
  ├─ identityVerificationMap(filter)
  ├─ revenueAdjustmentMap(filter)
  ├─ 过滤 RS Scheme Fee
  ├─ 生成 provider 明细
  └─ 生成 is_sum 汇总明细
```

这里不能简单根据“客户是否已经迁移”选择一个来源，因为当前生产中存在部分迁移客户。必须同时查询新、旧来源，再根据业务键避免重复计费。

#### 直客/新费率专用方法

```java
getAccountFeeMonthlySettlementFee(QueryFilterDTO filter)
```

该方法只查询 `account_fee_transaction`，主要条件是：

```text
deduction_node = MONTHLY
status = Closed
create_time >= 账单月开始
create_time < 下个月开始
fee_name 属于量子卡费用集合
business_id 关联 qbitCard
```

查询结果先由 Mapper 按以下维度汇总：

```text
account_id
fee_name
qbitCard.provider
```

然后通过：

```java
AccountFeeTransactionMonthlyFeeConverter.convert(...)
```

转换为 `ApiClientFeeEnum`，最后生成 `ApiClientMonthFeeVO`。

### 第四步：看直客 ActiveCardFee

仍在：

```text
ApiClientBillServiceImpl
```

主要方法：

```java
getActiveCardFeeTodos(...)
buildActiveCardFeeStatements(...)
buildActiveCardFeeStatement(...)
mapActiveCardProvider(...)
```

处理规则：

1. 查询表为 `accountToDo`。
2. 查询条件为 `key = 'ActiveCardFee'`。
3. 账单月份使用 JSON 中的 `billingMonth`。
4. 明细金额使用 `feeDetails[].amount`。
5. `feeDetails[].amount` 累加形成 `is_sum = true` 汇总行。
6. `BlueBancCardRecharge` 转换为 `BB`。
7. 该待办只作为费用来源，不再直接触发扣款。

### 第五步：看账单明细落库

直客落库方法：

```java
saveDirectBills(...)
toBillStatement(...)
```

落库关系：

```text
ApiClientBill
  └─ api_client_bill

ApiClientBillStatement
  └─ api_client_bill_statement
```

直客账单字段：

```text
type = MonthlyStatement
customer_type = DIRECT
check_status = Done
status = Pending 或 FREE
month_amount = 费用汇总
total_amount = 费用汇总
```

费用明细字段：

```text
item     ← 费用中文名称
type     ← ApiClientFeeEnum.value
amount   ← 费用金额
provider ← 供应商
is_sum   ← 是否汇总行
bill_id  ← 主账单 ID
```

### 第六步：看返现账单

文件仍是：

```text
ApiClientBillServiceImpl
```

主要方法：

```java
initRebate(InitRebateDTO initRebateDTO)
dealCashBackBonusHistoryData(...)
```

执行过程：

```text
create_rebate_bill
  └─ dealCashBackBonusHistoryData
       └─ initRebate
            ├─ 查询 CLOSED 状态返现明细
            ├─ 创建或更新 api_client_bill.type = Rebate
            ├─ 保存 bill_history_config
            ├─ 写入返现账单客户类型
            ├─ 更新 cash_back_bonuses.bill_id
            └─ 生成返现明细文件
```

直客同样可以进入返现账单流程，不应因为账户不是 API 类型而跳过 `Rebate`。

### 第七步：看 Net 轧差生成

文件：

```text
qbit-core/src/main/java/com/qbit/common_all/api/client/bill/service/impl/ApiClientNettingBillServiceImpl.java
```

入口：

```java
initApiClientNettingBill(InitNettingBillDTO initNettingBillDTO)
```

Net 的输入是同一账户、同一账单月份的：

```text
MonthlyStatement
Rebate，可为空
```

金额计算：

```text
账单金额 = MonthlyStatement 总额
返现金额 = Rebate 总额
```

状态分支：

```text
账单金额 = 返现金额
  → NettingEqual
  → 账单和返现均结清

账单金额 > 返现金额
  → NettingDebit
  → 产生待扣款金额

返现金额 > 账单金额
  → NettingRebate
  → 产生待返现金额
```

Net 不重复保存客户类型，而是通过：

```text
api_client_netting_bill.bill_id
  → api_client_bill.customer_type
```

### 第八步：看 Net 扣款

文件：

```text
qbit-core/src/main/java/com/qbit/common_all/api/client/bill/service/impl/ApiClientDebitRecordServiceImpl.java
```

主要入口：

```java
debitNetting(...)
isDirectCustomer(ApiClientNettingBill nettingBill)
dealDirectNettingDebit(...)
dealAutoMultiWalletNettingDebit(...)
```

处理顺序：

```text
debitNetting
  ├─ 初始化 Net
  ├─ 处理 NettingEqual
  ├─ 查询 Net 关联的 api_client_bill
  ├─ 读取 customer_type
  ├─ DIRECT → 直客透扣
  └─ API → 原 API 多钱包扣款
```

直客扣款规则：

```text
量子账户余额足够
  → 正常扣款

量子账户余额不足
  → 允许扣至负数
  → 账单结清
```

直客不再进入原 API 的：

```text
欠款通知
手动还款
负余额强制平账
```

### 第九步：看客户端返现/支付展示

方法：

```java
ApiClientBillServiceImpl.getRebateOrPay(...)
```

类型判断优先级：

```text
查询当月 MonthlyStatement.customer_type
  └─ 不存在时查询 Rebate.customer_type
       └─ 仍不存在时，兼容旧账户类型判断
```

新账单进入统一 Net 展示流程后，客户端根据 Net 类型展示：

```text
NettingEqual
  → 显示已结清

NettingDebit
  → 显示待支付/已支付/剩余支付金额

NettingRebate
  → 显示待返现金额
```

## 十二、关键表之间的关系

```text
account
  └─ account_id

account_fee_transaction
  ├─ account_id
  ├─ business_id → qbitCard.id
  ├─ transaction_id
  ├─ source_id
  ├─ fee_name
  ├─ fee
  └─ deduction_node/status

accountToDo
  ├─ accountId
  ├─ key = ActiveCardFee
  └─ value.feeDetails[].amount

api_client_bill
  ├─ account_id
  ├─ type = MonthlyStatement/Rebate
  ├─ customer_type = API/DIRECT
  └─ bill_history_config

api_client_bill_statement
  ├─ bill_id
  ├─ item/type/provider
  ├─ amount
  └─ is_sum

api_client_netting_bill
  ├─ account_id
  ├─ bill_id
  ├─ rebate_id
  ├─ type
  ├─ amount
  └─ status

api_client_debit_record
  ├─ bill_id
  ├─ debit_amount
  ├─ real_amount
  └─ debit_type
```

## 十三、出现问题时的排查顺序

### 账单没有生成

1. 检查 XXL-JOB 是否执行。
2. 检查任务参数中的 `accountIds` 和 `billMonths`。
3. 查询 `account_fee_transaction` 是否满足 `MONTHLY + Closed`。
4. 查询 `accountToDo.ActiveCardFee` 的 `billingMonth` 是否匹配。
5. 检查账户是否被识别为 API 账户。
6. 检查 `api_client_bill` 是否已经存在同月份账单。

### 账单金额不正确

1. 查询 `api_client_bill_statement` 明细。
2. 区分 `is_sum = true` 汇总行和 provider 明细行。
3. 检查 `account_fee_transaction.fee_name` 映射。
4. 检查 `ActiveCardFee.feeDetails[].amount` 是否重复计入。
5. 检查 RS Scheme Fee 是否被过滤。

### 直客被错误进入 API 扣款

按以下链路查询：

```sql
SELECT
    nb.id AS netting_bill_id,
    nb.bill_id,
    b.customer_type,
    b.type AS bill_type,
    nb.type AS netting_type,
    nb.status AS netting_status
FROM api_client_netting_bill nb
LEFT JOIN api_client_bill b ON b.id = nb.bill_id
WHERE nb.account_id = :accountId
  AND nb.bill_month = :billMonth;
```

重点确认 `b.customer_type = 'DIRECT'`。

### CSV 没有明细

1. 根据账单的 `customer_type` 确认走 API 还是直客导出。
2. API 检查 `api_client_transaction` 和 `account_fee_transaction`。
3. 直客检查 `account_fee_transaction` 是否有量子卡关联。
4. 检查 ActiveCardFee 是否生成独立明细文件。
5. 检查 ZIP 生成和 OSS 上传日志。

## 十四、当前代码与文档的边界

以下内容不属于本次直客账单代码入口：

- 全球账户费用账单。
- 加密资产费用账单。
- Flink 对 `api_client_bill` 的下游同步逻辑。
- 财务系统对账脚本。
- XXL-JOB 管理后台的实际 Cron 配置。

这些系统只能依赖新增的 `api_client_bill.customer_type` 识别账单生成时客户类型，不能重新读取当前账户类型覆盖历史值。
